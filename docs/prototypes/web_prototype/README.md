# web_prototype (experimental)
This folder contains an experimental static web prototype (HTML/CSS/JS) kept for reference.

## Status
Experimental: not part of the production runtime; no guarantees of maintenance.

## How to run
Open `index.html` in a browser, or serve the directory with any static file server.
